


for (int i = 0; i < 20; i++)
{
	for (int j = 0; j < 20; j++)
	{
		Console.Write("[ ]");
	}

	Console.WriteLine();
}

// I don't like that the grid is so large.
// Let's expirement with some new ideas...
// BUT let's expirement on a NEW BRANCH!

// git checkout -b changeGridSize (this is shorthand for creating a new branch, and checking it out)